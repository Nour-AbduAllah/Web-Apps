@model OnlineShop.Models.SpecialTag

@{
    ViewData["Title"] = "Details";
}
<div>
    <h3 class="text-info">Special Tag Details</h3>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.ID" class="btn btn-primary"></a> |
    <a asp-action="Index" class="btn btn-outline-success"><i class="fas fa-home"></i></a>
</div>
